# Backend Configuration
# Energy Charts API bidding zone (default: DE-LU for Germany/Luxembourg)
# Other options: NL (Netherlands), FR (France), DK-1 (Denmark West), etc.
BZN=DE-LU

# Timezone for price calculations and display (default: Europe/Amsterdam)
TIMEZONE=Europe/Amsterdam

# Energy Charts API base URL (should not need to change)
PRICES_API_BASE=https://api.energy-charts.info

# Frontend Configuration
# Backend API URL (used by frontend to connect to backend)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api